public class SymbolR {
    public static boolean symbolR (char line) {
        if (line >= '0' && line <= '9' || line == 'I' || line == 'V' || line == 'X' || line == '+' || line == '-' || line == '*'||
        line == '/');
        return true;
    }
}
